//: [Previous](@previous)

import Foundation

// Enum: a set of named values we can create and use to make our code more efficient and safer

enum Weekday {
    case monday, tuesday, wednesday, thursday, friday
}

var day = Weekday.monday
day = .friday
